
============= 圖形 (Graph) 核心概念與筆記 ===============

1. 基本組成：
   - 頂點(Vertex / Node)：圖形中的資料節點
   - 邊(Edge / Link)：連接兩個頂點的線(代表它們之間的關係)

2. 常見分類：
   - 有向圖(Directed) vs. 無向圖(Undirected)：邊是否有方向性
     (本類別實作的是無向圖，因此 addEdge 時兩個頂點都要互相加入對方)
   - 有權重(Weighted) vs. 無權重(Unweighted)：邊是否帶有數值(如：距離、成本)
   - 有環圖(Cyclic) vs. 無環圖(Acyclic)：從一個頂點出發是否能走回自己
     (樹 Tree 其實就是一種特殊的 "無環連通無向圖")

3. 圖形的兩種經典實作方式：
   A. 鄰接矩陣 (Adjacency Matrix)：
      - 使用二維陣列 (2D Array) 實作
      - 空間複雜度 O(V^2)，適合「稠密圖 (Dense Graph)」(邊很多的圖)
      - 尋找兩點是否有連線極快 O(1)，但新增/刪除頂點極慢 O(V^2)

   B. 鄰接串列 (Adjacency List)：本類別採用的實作方式
      - 使用 HashMap 搭配 ArrayList (或 LinkedList) 實作
      - 空間複雜度 O(V + E)，適合現實中最常見的「稀疏圖 (Sparse Graph)」
      - 尋找某頂點的所有相連邊非常快速，且新增頂點只需 O(1)

4. 本類別 (Adjacency List) 的時間複雜度：
   - 新增頂點 (Add Vertex)：O(1)
   - 新增邊 (Add Edge)：O(1) (將元素加到 ArrayList 尾端)
   - 刪除邊 (Remove Edge)：O(E) (需要遍歷 ArrayList 找到該頂點並移除)
   - 刪除頂點 (Remove Vertex)：O(V + E) (需先去所有相連頂點的 List 中刪除自己，最後再除掉自己的 Key)

